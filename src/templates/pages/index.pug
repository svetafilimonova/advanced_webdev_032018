extends ../template

block variables
    - var title = 'Добро пожаловать'

block content
    h1 Главная мышь мышня
    form
        label(for="email") email
        input(name="email" id="email")
        label(for="password") password
        input(name="password" id="password")
        button Войти